# A built-in data types that lets us create immutable sequence of values
# Syntax : tup=()
# Tuples are ordered and immutable
# Tuples are defined using parentheses ()

tup=(2,4,5,2,3,2,2)
t=tup.index(2)  # if find  the element then return the index of the element
print(t)

c=tup.count(2) # count of toatal occuranace
print(c)


b={"name":"Vishal"}
print(type(b))